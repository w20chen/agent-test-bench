[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-sched-bench"
version = "0.1.0"
description = "Benchmark environment for agent scheduling research"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "hydra-core>=1.3,<2.0",
  "httpx>=0.27,<1.0",
  "openai>=2.0,<3.0",
  "PyYAML>=6.0,<7.0",
  "trafilatura>=2.0,<3.0",
  "tqdm>=4.66,<5.0",
  "datasets>=3.0,<4.0",
  "loguru>=0.7,<1.0",
  "pydantic>=2.0,<3.0",
  "json-repair>=0.30,<1.0",
  "tiktoken>=0.7,<1.0",
  "terminal-bench>=0.2.18; python_version >= '3.12'",
  # Spike Q3 (kv_cache_subclass_spike.md): SinkCache became a stub in 4.58
  # and the DynamicCache subclass contract held on 4.55-4.57. Keep the upper
  # bound until 4.58+ is re-validated.
  "transformers>=4.55,<4.58",
  # AutoModelForCausalLM.from_pretrained(device_map="auto") raises when
  # accelerate is missing. The HF recording backend always uses device_map,
  # so accelerate is a hard runtime dep, not optional.
  "accelerate>=1.0,<2.0",
]

[project.optional-dependencies]
mcp = ["mcp>=1.0"]
profile = ["vllm>=0.5,<0.8", "torch>=2.1"]
dev = [
  "pytest>=8.3,<9.0",
  "pytest-asyncio>=0.26,<1.0",
  "ruff>=0.11,<1.0",
]
gantt-viewer = [
  "fastapi>=0.115,<0.120",
  "uvicorn[standard]>=0.30,<0.40",
  "python-multipart>=0.0.9,<0.1",
  "anyio>=4.0,<5.0",
]
# Optional HTML-parsing deps retained for ad-hoc trace inspection tools.
gantt-test = [
  "beautifulsoup4>=4.12",
  "lxml>=5.0",
]
# Runtime deps of the vendored Tongyi-DeepResearch scaffold
# (src/agents/tongyi_deepresearch/). Not pulled into the default install
# because alibabacloud-docmind-api20220711 transitively brings ~8 alibabacloud
# packages. Install explicitly: `pip install -e .[tongyi]`.
tongyi = [
  "qwen-agent>=0.0.30,<1.0",
  "sandbox-fusion>=0.3,<1.0",
  "alibabacloud-docmind-api20220711>=1.4,<2.0",
  "beautifulsoup4>=4.12,<5.0",
  "ffmpeg-python>=0.2,<1.0",
  "json5>=0.9,<1.0",
  "lxml>=5.0,<6.0",
  "pandas>=2.0,<3.0",
  "pdfminer.six>=20221105",
  "pdfplumber>=0.11,<1.0",
  "Pillow>=10.0,<12.0",
  "python-docx>=1.1,<2.0",
  "python-pptx>=1.0,<2.0",
  "requests>=2.31,<3.0",
  "scenedetect>=0.6,<1.0",
  "tabulate>=0.9,<1.0",
  "transformers>=4.40,<5.0",
]

[project.scripts]
openclaw = "agents.openclaw._cli:main"

[tool.setuptools]
package-dir = {"" = "src", "demo" = "demo"}

[tool.setuptools.packages.find]
where = ["src", "."]
include = [
  "llm_call*",
  "trace_collect*",
  "agents*",
  "serving*",
  "harness*",
  "demo*",
]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests", "demo/gantt_viewer/tests"]
markers = [
  "slow: marks tests that hit real on-disk fixtures and may be skipped in fast CI runs",
]

[tool.ruff]
line-length = 88
target-version = "py311"
